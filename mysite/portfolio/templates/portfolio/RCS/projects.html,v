head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2013.06.18.03.06.04;	author dmf;	state -;
branches;
next	;


desc
@@


1.1
log
@-
@
text
@{% comment %}
 *
 * $Id: portfolio.html,v 1.1 2013-05-30 23:46:52-07 dmf - $
 * Derek Frank (dmfrank@@gmx.com)
 *
 * NAME
 *     projects.html
 *
 * DESCRIPTION
 *     Portfolio projects HTML template.
 *
{% endcomment %}

<h3>Projects:</h3>
<ul>
    {% for project in projects %}
        {% if project.url %}
            <h4 class="project">
                <a href="{{ project.url }}"
                   title="Visit the website for {{ project.title }}">
                    <span>{{ project.title }}</span>
                </a>
            </h4>
        {% else %}
            <h4 class="project">{{ project.title }}</h4>
        {% endif %}
        <span class="timestamp">{{ project.date }}</span>
        <ul><span>{{ project.description|linebreaks }}</span></ul>
        <ul>
        {% if project.repository_url %}
            <h5 class="inline">Repository:</h5>
            <a href="{{ project.repository_url }}"
               title="Visit {{ project }} repository on {{ project.get_repository_name_display }}">
                <span>{{ project.get_repository_name_display }}</span>
            </a>
        {% endif %}
        {% if project.language %}
            <h5>Language(s):</h5>
            <ul>
                {% for lang in project.language.all  %}
                    <h5 class="inline">{{ lang.get_type_display }} language:</h5>
                    <span>{{ lang }}</span>
                {% endfor %}
            </ul>
        {% endif %}
        </ul>
    {% endfor %}
</ul>

@
