head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2013.05.31.06.46.52;	author dmf;	state -;
branches;
next	;


desc
@@


1.1
log
@-
@
text
@{% extends "base.html" %}
{% comment %}
 *
 * $Id: mff.html,v 1.3 2013-05-23 20:05:51-07 dmf - $
 * Derek Frank (derekmfrank@@gmail.com)
 *
 * NAME
 *     mff.html
 *
 * DESCRIPTION
 *     MFF HTML template.
 *
{% endcomment %}
{% load static from staticfiles %}
{% load url from future %}

{% block head_title %}{{ mff }} | {{ mff.title }}{% endblock %}

{% block navbar_class %}nb-mff{% endblock %}

{% block body_inner_content %}
    <div id="content-left">
        <div class="box" style="min-height: 340px;">
            <h2>{{ mff }} | {{ mff.title }}</h2>
            <div class="center" >
                <a class="block" href="{% url "mff" %}" title="{{ mff.image_text }}">
                    <img  src="{% static mff.image %}" alt="Image for {{ mff }}" />
                </a>
                <span class="italic">{{ mff.image_text }}</span>
            </div>
            <div class="">
                {% for topic_bits in topics %}
                {% with topic_bits|first as topic %}
                {% with topic_bits|last as bits %}
                    <div class="topic clearfix">
                        <h3>{{ topic }}</h3>
                        {% if topic.pretopic_list %}
                            <span>{{ topic.pretopic_list|linebreaks }}</span>
                        {% endif %}
                        <ul>
                            {% for bit in bits %}
                                <li>{{ bit }}</li>
                            {% endfor %}
                        </ul>
                        {% if topic.body %}
                            <span>{{ topic.body|linebreaks }}</span>
                        {% endif %}
                    </div>
                {% endwith %}
                {% endwith %}
                {% endfor %}
                </div>
        </div>
    </div>
  
    <div id="content-right" >
        <div class="box">
            <h3>Contact Information</h3>
            <ul>
                <span class="block">{{ mff }}</span>
                <span class="block">{{ mff.title }}</span>
                <a class="block" href="mailto:{{ mff.email }}" 
                   title="Contact {{ mff }} via {{ mff.email }}">{{ mff.email_name }}</a>
                <span class="block">{{ mff.contact|linebreaks }}</span>
            </ul>
            <h3>Other Sources</h3>
            <ul>
                {% for src in mff.sources.all %}
                    <a class="block" href="{{ src.url }}">{{ src.title }}</a>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
@
